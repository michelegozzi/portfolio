<% provide(:title, "New Personal Detail") %>

<div class="row">
  <div class="breadcrumb-layout">
    <ul class="breadcrumb">
      <li>
        <%= link_to "Back", :back %>
      </li>
    </ul>
  </div>
</div>

<%= form_for @personal_detail do |f| %>
  <%= render 'fields', f: f %>
<% end %>